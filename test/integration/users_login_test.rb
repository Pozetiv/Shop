require 'test_helper'

class UserLoginTest < ActionDispatch::IntegrationTest

  #def setup
   # @user = users(:tester)
  #end

  #test "login with valid date" do
   # get login_path
   # post login_path, params: {session: {email: @user.email, password: 'tester12345'}}
    #assert_redirect_to @user
    #assert is_logged_in?
    #follow_redirect!
    #assert_template 'users/show'
    #assert_select "a[href=?]", login_path, count: 0
    #assert_select "a[href=?]", logout_path
  #end
end