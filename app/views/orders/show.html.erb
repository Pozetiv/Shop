

<% if @order.any?%>
    <table class="table">
      <thead>
      <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Item price $</th>
      </tr>
      </thead>
      <tbody>
      <% @order.each do |item| %>
          <tr>
            <td><%=item.order_items %></td>

          </tr>
      <% end %>
      <td>Total Price: <%= @orders.total_price  %>$</td>
      </tbody>
    </table>

    <% if @order.any? %>
        <table class="table">
          <thead>
          <tr>
            <th>Date create order</th>
            <th>Items name</th>
            <th>Total price order</th>
          </tr>
          </thead>
          <tbody>
          <% @order.each do |item| %>
             <tr>
               <td><%=item.created_at.to_date.strftime("%d %B, %Y") %></td>
               <td><%=item.products %></td>
             </tr>
        <% end %>
          </tbody>
        </table>
        <% end %>



<% else %>
    Your haven't got order
<% end %>

<%= link_to 'Order builde #{s}' %>

